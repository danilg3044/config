server:
  port: ${PADES_SERVER_PORT:8082}

tsa: http://timestamp.globalsign.com/scripts/timstamp.dll

sign:
  server: http://localhost:10434/WebApp/Signature.aspx
  
default:
  validation:
    policy: policy/constraint.xml
    
management:
  endpoints:
    web:
      exposure:
        include:
        - refresh    